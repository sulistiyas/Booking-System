import './bootstrap';
import Alpine from 'alpinejs';

window.Alpine = Alpine;

// alpine components

// alpine pages

// register alpine pages

// register alpine components

// start alpine
Alpine.start();